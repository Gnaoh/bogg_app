<%= form_for @creature do |f| %>
	
	<%= f.text_field :name%>
	<%= f.text_area :description %>
	<%= f.submit "save creature" %>
	<%= link_to "Back", "/creatures", class: "btn btn-default btn-sm" %>
<% end %>